
# Application Name
spring.application.name=cam

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/cam_db?useSSL=false&serverTimezone=UTC


# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cam_db

spring.datasource.username=root
spring.datasource.password=catpoocha
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pooling (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# JPA Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.username=root
spring.datasource.password=catpoocha


# Logging Configuration
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# # Spring Security (Disable CSRF for testing)
# spring.security.user.name=admin
# spring.security.user.password=admin123

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

